package dev.gooo.finance.api.loan.model;

public enum InterestTypeEnum {
    /**
     * fixed: Fixed interest (固定金利)
     */
    FIXED,
    /**
     * variable: Variable(floating) interest (変動金利)
     */
    VARIABLE,
}
